services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      APP_ENV: prod
      SECRET_KEY: ${SECRET_KEY}
      DATABASE_URL: ${DATABASE_URL}
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
